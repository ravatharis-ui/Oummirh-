/**
 * Public API of the `demo` module.
 *
 * Other modules may import from here and nowhere else inside this folder.
 * ESLint (`boundaries/dependencies`) enforces that rule.
 */
export { demoModule } from "./manifest";
