# Module `swaps` — Échanges de créneaux

_Implémenté en Phase 9._

- **Rôle** : à compléter.
- **Tables** : voir PROMPT.md §5.
- **Événements émis** : à compléter.
- **Événements écoutés** : à compléter.
- **Écrans** : `ui/collab/`, `ui/admin/`.

Structure imposée : `index.ts` (seule API publique), `manifest.ts`, `schemas.ts`, `types.ts`,
`domain/` (logique pure), `server/`, `ui/`, `emails/`, `__tests__/`.
