# core/db

Clients Supabase (`browser`, `server`, `admin` — service_role côté serveur uniquement) et types générés
`database.types.ts` (régénérés via `npm run db:types` après chaque migration). Implémenté en Phase 1.
